apiVersion: v1
kind: Service
metadata:
  name: {{ include "wms_chart.fullname" . }}
  labels:
    {{- include "wms_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wms_chart.selectorLabels" . | nindent 4 }}
