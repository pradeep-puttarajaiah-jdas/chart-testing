apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wms_chart.fullname" . }}-test-connection"
  labels:
    {{- include "wms_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wms_chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
